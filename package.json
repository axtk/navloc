{
  "name": "@axtk/router",
  "version": "1.2.7",
  "description": "A lightweight browser history router",
  "main": "dist/index.js",
  "scripts": {
    "build": "esbuild dist/index.js --bundle --outfile=index.build.js --global-name=Router",
    "build:min": "npm run build -- --minify",
    "tsc": "tsc"
  },
  "keywords": [
    "router",
    "route",
    "history"
  ],
  "repository": {
    "type": "git",
    "url": "git://github.com/axtk/router.git"
  },
  "author": "axtk",
  "license": "MIT",
  "dependencies": {
    "@axtk/event-manager": "^1.2.0"
  },
  "devDependencies": {
    "esbuild": "^0.8.52",
    "typescript": "^4.2.3"
  },
  "publishConfig": {
    "access": "public"
  }
}
