{
  "name": "@axtk/router",
  "version": "1.2.3",
  "description": "A lightweight browser history router",
  "main": "index.js",
  "scripts": {
    "build": "npx esbuild index.js --bundle --outfile=index.build.js --global-name=Router",
    "build:min": "npm run build -- --minify"
  },
  "keywords": [
    "router",
    "route",
    "history"
  ],
  "repository": {
    "type": "git",
    "url": "git://github.com/axtk/router.git"
  },
  "author": "axtk",
  "license": "MIT",
  "dependencies": {
    "@axtk/event-manager": "^1.1.3"
  },
  "devDependencies": {
    "esbuild": "^0.8.52"
  },
  "publishConfig": {
    "access": "public"
  }
}
